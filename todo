Make the nav aside function as javascript popup

"I write text in 2 styles, recipe and blog. blog text goes to tietokanta and  anywhere else that needs it, but recipe text goes always to reseptit. make the non-recipe text"
